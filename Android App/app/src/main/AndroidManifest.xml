<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.piti.draku.piti">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreensActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"

            android:screenOrientation="portrait"/>

        <activity android:name=".MainParentActivity"/>
        <activity android:name=".MainParentActivity2"/>
        <activity android:name=".MainParentActivity3"/>
        <activity android:name=".ParentNotificationActivity"/>
        <activity android:name=".ParentStudentActivity"/>
        <activity android:name=".ParentMarkActivity"/>
        <activity android:name=".ParentScheduleActivity"/>
        <activity android:name=".ParentAskActivity"/>

        <activity android:name=".MainTeacherActivity"/>
        <activity android:name=".MainTeacherActivity2"/>
        <activity android:name=".MainTeacherActivity3"/>
        <activity android:name=".TeacherProfileActivity"/>
        <activity android:name=".TeacherScheduleActivity"/>
        <activity android:name=".TeacherNotificationActivity"/>
        <activity android:name=".TeacherAttendanceActivity"/>
        <activity android:name=".TeacherMarkActivity"/>
        <!-- [START firebase_service] -->
        <service
            android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <!-- [END firebase_service] -->
        <!-- [START firebase_iid_service] -->
        <service
            android:name=".MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>
        <!-- [END firebase_iid_service] -->
    </application>

</manifest>